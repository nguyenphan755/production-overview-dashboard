# Production build — copy to `.env.production` before `npm run build`.
# Only VITE_* (embedded at build time). No secrets.

# Same-origin behind nginx (example UI on http://host:8080, nginx proxies /api):
# VITE_API_BASE_URL=http://localhost:8080/api

# Split processes (pm2 serve UI + API on another port) — use absolute API URL:
VITE_API_BASE_URL=http://127.0.0.1:3001/api

VITE_USE_MOCK_DATA=false
VITE_REALTIME_ENABLED=true

# Grafana embed (Equipment Detail + Speed Lab tabs)
VITE_GRAFANA_URL=http://localhost:3002
# Recommended for ~20 factory users (reduces /api/machines load vs default 1s)
VITE_POLL_MS_MACHINES=2000
VITE_POLL_MS_MACHINE_DETAIL=5000
# VITE_DEBUG_API=false

# Speed Lab downtime table/export: min zero-speed segment in seconds (default 60 = ≥1 phút).
# VITE_MIN_DOWNTIME_STOP_SEC=60

# EquipmentDetail — Energy per interval: meter = ưu tiên bảng energy_consumption (fallback từ kW nếu trống).
# power = luôn ước lượng từ công suất (kW) × độ dài bucket — chấp nhận sai số; bỏ qua energy_consumption cho biểu đồ theo giờ.
# VITE_ENERGY_SOURCE=power
